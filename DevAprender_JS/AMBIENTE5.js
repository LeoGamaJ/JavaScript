// Abecedário

/*
function solucao(letra, palavras) {
	//var letra = 'j';
    //var palavras = ['mamão'];
    if (letra != palavras[0]) {
        resulPerdeu = letra != palavras
        console.log(Number(resulPerdeu));
    } else {
        console.log('GANHOU')
    }
}

solucao('m', ['mamão', 'açaí'])
*/
/*
function solucao(letra, palavras) {
	let letra = 'j';
    let palavras = ['mamão'];
    let perdeu = letra ! palavras[0][0];

    console.log(perdeu)
};
*/

function solucao(letra, palavras) {
    letra = '' ;
    palavras [''];
    
    while (letra !== palavras[0][0]) {
        result = palavras !== letra;
        result += 1;
        return console.log(result)

    }
    


    /*
    if (letra != palavras[0]) {
        result = palavras[0] !== letra;
        result += 1;
        console.log(result)
    }
    */
}
solucao('a', ['mamão', 'açaí'])